# BSM Member System - Local Development Environment
# Copy this file to .env.local and fill in your values

# =============================================================================
# DEVELOPMENT MODE FLAGS
# =============================================================================
NODE_ENV=development
USE_LOCAL_DYNAMODB=true
USE_LOCAL_STORAGE=true

# =============================================================================
# AWS CONFIGURATION (Local DynamoDB)
# =============================================================================
AWS_REGION=ap-southeast-2
DYNAMODB_ENDPOINT=http://localhost:8000
DYNAMODB_TABLE_NAME=bsm-members-local
DYNAMODB_ADMIN_LOGS_TABLE=bsm-admin-logs-local

# For local development, use dummy AWS credentials
AWS_ACCESS_KEY_ID=local
AWS_SECRET_ACCESS_KEY=local

# =============================================================================
# NEXTAUTH CONFIGURATION
# =============================================================================
NEXTAUTH_URL=http://localhost:3000
# Generate a secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key-minimum-32-characters-long-change-this

# =============================================================================
# GOOGLE OAUTH (Development)
# =============================================================================
# Get from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID with authorized origins: http://localhost:3000
NEXT_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# FILE STORAGE (Local)
# =============================================================================
UPLOAD_DIR=public/uploads/photos

# =============================================================================
# APPLICATION URLs
# =============================================================================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Bengali Society of Melbourne
NEXT_PUBLIC_CONTACT_EMAIL=info@bsm.org.au

# =============================================================================
# EXISTING FEATURES (Keep these from your current .env.local)
# =============================================================================
# Google Maps API for membership address autocomplete
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-existing-google-maps-api-key

# Web3Forms for contact form (can keep for now, will migrate later)
NEXT_PUBLIC_WEB3FORMS_KEY=your-web3forms-key
