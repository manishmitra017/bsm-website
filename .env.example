# BSM Monorepo Environment Variables
# Copy this file to .env.local and fill in the values

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Google Maps API Configuration
# Get your API key from: https://console.cloud.google.com/
# Required APIs: Maps JavaScript API, Places API, Geocoding API
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Bengali Society of Melbourne
NEXT_PUBLIC_CONTACT_EMAIL=info@bsm.org.au

# Email Configuration (for contact form)
EMAIL_SENDER=noreply@bsm.org.au
CONTACT_EMAIL=info@bsm.org.au

# =============================================================================
# BACKEND CONFIGURATION (Future use)
# =============================================================================

# Database Configuration
DATABASE_URL=your-database-connection-string
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bsm_website
DB_USER=bsm_user
DB_PASSWORD=your-db-password

# API Configuration
API_BASE_URL=http://localhost:8000
API_SECRET_KEY=your-api-secret-key
JWT_SECRET=your-jwt-secret-key

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10MB

# =============================================================================
# SHARED CONFIGURATION
# =============================================================================

# AWS Configuration (for production deployment)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=ap-southeast-2

# Production Domain (used by SST for production deployment)
PRODUCTION_DOMAIN=bsmmelbourne.org

# Node Environment
NODE_ENV=development

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env.local for local development
# 2. Replace all 'your_*' placeholders with actual values
# 3. For Google Maps API:
#    - Enable Maps JavaScript API, Places API, and Geocoding API
# 4. For AWS deployment:
#    - Create an AWS account and get access keys
#    - Install AWS CLI: aws configure
# 5. For Backend (when implemented):
#    - Set up PostgreSQL/MySQL database
#    - Configure API endpoints and secrets
# 6. Restart servers after making changes